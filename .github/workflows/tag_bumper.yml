on:
  push:
    branches:
      - main
    tags:
      - 'v?*.*.*'
  pull_request:
    branches:
      - main
    types:
      - labeled
      - unlabeled
      - opened
      - reopened
      - synchronize

permissions:
  pull-requests: write
  contents: read

jobs:
  tag_bumper:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - id: bumper
        uses: ./
        with:
          fail_if_no_bump: true
          bump_semver: true
          add_latest: true
          github_token: ${{ secrets.GITHUB_TOKEN }}
